{
  "description": "数据集去重工具 | Dataset Deduplication Tool",
  "b": "dataSourceType",
  "properties": [
    {
      "title": "数据来源 | Data Source",
      "name": "dataSourceType",
      "type": "string",
      "editor": "select",
      "description": "选择数据来源类型 | Select data source type",
      "default": "direct-input",
      "options": [
        { "label": "直接输入 | Direct Input", "value": "direct-input" },
        { "label": "网络URL | Network URL", "value": "network-url" }
      ],
      "required": true
    },
    {
      "title": "输入数据 | Input Data",
      "name": "inputData",
      "type": "string",
      "editor": "textarea",
      "description": "JSON数组数据 | JSON array data",
      "required": false
    },
    {
      "title": "数据URL | Data URLs",
      "name": "inputUrls",
      "type": "array",
      "editor": "requestList",
      "description": "数据文件URL列表 | Data file URL list",
      "default": []
    },
    {
      "title": "去重字段 | Dedup Fields",
      "name": "fields",
      "type": "array",
      "editor": "stringList",
      "description": "去重字段名称 | Field names for deduplication",
      "default": [
        { "string": "id" }
      ],
      "required": true
    },
    {
      "title": "输出内容 | Output",
      "name": "output",
      "type": "string",
      "editor": "select",
      "description": "输出类型 | Output type",
      "default": "unique-items",
      "options": [
        { "label": "唯一项 | Unique", "value": "unique-items" },
        { "label": "重复项 | Duplicates", "value": "duplicate-items" }
      ]
    },
    {
      "title": "处理模式 | Mode",
      "name": "mode",
      "type": "string",
      "editor": "select",
      "description": "去重模式 | Dedup mode",
      "default": "dedup-after-load",
      "options": [
        { "label": "先加载后去重 | After Load", "value": "dedup-after-load" },
        { "label": "边加载边去重 | As Loading", "value": "dedup-as-loading" }
      ]
    }
  ]
}
